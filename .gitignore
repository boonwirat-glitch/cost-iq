# Dev-only local preview server (referenced by ~/.claude launch config, not part of the app)
_preview_server.cjs

# OS noise
.DS_Store

# Build output (regenerated by build.py — the deployed copy is index.html/nrr.html
# at repo root, not dist/; committing every version snapshot bloated .git needlessly)
dist/
