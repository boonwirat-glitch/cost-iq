# Handoff — 2026-06-20 — v820

## สิ่งที่ทำสำเร็จ

- **v819** cohort sub-row double count fix — `claimedByEarlierCohort` ป้องกัน outlet นับซ้ำข้าม cohort
- **v819a** unified cohort color — ทุก cohort ใช้ `rgba(96,165,250,.72)` เหมือนกัน
- **v820** console log cleanup — ลบ 80+ legacy/verbose debug lines

---

## สิ่งที่ยังไม่สำเร็จ

### SQL quarterly NRR — TL/Admin scope

**ไฟล์:** `sql/quarterly_nrr_2026_Q2_tl_admin.sql`

**เป้าหมาย:** แยก SQL สำหรับ TL/Admin ออกจาก rep (v5) โดยใช้ `commercial_owner` เป็นหลักแทน `staff_owner` เพื่อให้ outlet ของ KAM ที่ลาออกยังนับเป็น core cohort ได้

**Ground truth ที่ verified แล้ว:**
- Mar KAM cohort จริง = 2,721 outlets ฿137.3M (`commercial_owner = 'KAM'`)
- handover_in_mar = 25 outlets (new_user_exp_date = Mar 2026 เท่านั้น)
- core cohort เป้าหมาย = 2,696 outlets ฿133.8M
- KAM ลาออก 5 คน = 496 outlets ยังเป็น `commercial_owner = 'KAM'` ทั้งหมด → ควรนับเป็น core

**ปัญหาที่ยังแก้ไม่ได้:**
- SQL ปัจจุบันจับ core cohort ได้แค่ 808 outlets (เป้า 2,696)
- outlet 496 ตัวของ KAM ลาออกตก `comeback` แทน `core_nrr`
- `period_kam_email = NULL` 249 rows ใน Apr

**Root cause ที่ยังไม่ได้ยืนยัน:**
- `mar_cohort` CTE อาจไม่ได้จับ outlet ของ KAM ลาออกครบ แม้ใช้ LEFT JOIN แล้ว
- ต้อง verify ด้วย query นี้ก่อน session หน้า:

```sql
WITH params AS (
  SELECT DATE('2026-03-01') AS base_start, DATE('2026-03-31') AS base_end
),
base_gmv AS (
  SELECT CAST(o.user_id AS STRING) AS outlet_id,
         ROUND(SUM(o.gmv_ex_vat),0) AS gmv
  FROM `freshket-rn.dwh.order` o CROSS JOIN params p
  WHERE o.delivery_date BETWEEN p.base_start AND p.base_end
    AND o.gmv_ex_vat > 0 AND o.account_type IN ('SA','MC','Chain','Unknown')
  GROUP BY 1
),
mar_ownership AS (
  SELECT CAST(o.user_id AS STRING) AS outlet_id,
         UPPER(TRIM(o.commercial_owner)) AS commercial_owner,
         TRIM(o.staff_owner) AS staff_owner,
         DATE(o.new_user_exp_date) AS new_user_exp_date
  FROM `freshket-rn.dwh.order` o CROSS JOIN params p
  WHERE o.delivery_date BETWEEN p.base_start AND p.base_end
    AND o.account_type IN ('SA','MC','Chain','Unknown') AND o.user_id IS NOT NULL
  QUALIFY ROW_NUMBER() OVER (PARTITION BY o.user_id ORDER BY o.delivery_date DESC) = 1
),
kam_list AS (
  SELECT kam_name FROM UNNEST([
    'Anusorn (Bookbig) Khamphasuk','Chaklid (Dent) Nimraor',
    'Duangruedee (Ning) Bulalom','Guntinun (Monet) Thanoochan',
    'Intuon (Jane) Yanakit','Napat (To) Kaikaew','Natchita (Foam) Bunkong',
    'Niracha (Cream) Sangka','Nuttawan (Kwang) Mahaporn',
    'Ploynitcha (Nitcha) Rujipiromthagoon','Puttipong (Tape) Wanithaweewat',
    'Rinlaphat (Mild) Setthasiriwuti','Siriprapa (Pop) Piapeng',
    'Treerak (May) Sangjua','Warissara (Ply) Chanaboon'
  ] AS kam_name)
)
SELECT
  CASE WHEN k.kam_name IS NOT NULL THEN 'in_roster' ELSE 'left_roster' END AS roster_status,
  COUNT(DISTINCT mo.outlet_id) AS outlet_count,
  ROUND(SUM(bg.gmv),0) AS mar_gmv
FROM mar_ownership mo
LEFT JOIN kam_list k ON TRIM(mo.staff_owner) = k.kam_name
LEFT JOIN base_gmv bg ON mo.outlet_id = bg.outlet_id
WHERE mo.commercial_owner = 'KAM'
  AND COALESCE(bg.gmv,0) > 0
  AND NOT (FORMAT_DATE('%Y-%m', mo.new_user_exp_date) = '2026-03')
GROUP BY 1 ORDER BY 1;
```

ถ้า left_roster ได้ ~496 outlets → mar_cohort ถูกต้อง ปัญหาอยู่ที่ LEG A/B/C
ถ้า left_roster ได้ 0 → mar_cohort ไม่จับ KAM ลาออกเลย ต้องแก้ CTE

---

## ข้อผิดพลาดที่ทำใน session นี้

1. **Patch วนซ้ำโดยไม่ verify root cause** — แก้ SQL หลายรอบโดยไม่รู้ว่า mar_cohort จับ outlet ได้จริงกี่ตัว ควร verify mar_cohort ก่อนแตะ LEG ใดๆ
2. **สร้างไฟล์ SQL หลายเวอร์ชั่น** (v5, v6, v7, v8, tl_admin) โดยไม่ได้ validate ให้ครบก่อน push — ทำให้ git รกและเสียเวลา
3. **โยน diagnostic SQL ขึ้น git** — ควรรันใน session เท่านั้น
4. **คิดแบบ incremental แทนที่จะ verify ground truth ก่อน** — ควร verify mar_cohort count ตั้งแต่ต้น session

---

## สถานะปัจจุบัน

| ไฟล์ | สถานะ |
|---|---|
| `sql/quarterly_nrr_2026_Q2_v5.sql` | ✅ stable — ใช้สำหรับ rep scope |
| `sql/quarterly_nrr_2026_Q2_tl_admin.sql` | ❌ ยังไม่ถูกต้อง — core cohort 808 แทน 2,696 |
| `src/07c_qnrr_view.js` | ✅ v820 — cohort double count fixed |

## App version: v820
