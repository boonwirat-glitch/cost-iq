# Handoff — 2026-06-18 — v797 + SQL v4b

## App version
- **Build:** v797 (sense-v797)
- **Deploy:** Cloudflare Pages — freshket-sense.pages.dev
- **Commit:** d5e06fc14a

---

## งานที่ทำใน session นี้

### 1. QNRR Bar Chart — Partial Month (Jun)

**Ghost bar (Option A refined)**
- Ghost = dashed outline แสดงเฉพาะส่วนที่เกิน actual bar ขึ้นไปเท่านั้น (ไม่ครอบทั้ง column)
- `.qnrr-ghost-top` — position absolute, bottom=barH, height=gapH, border-bottom:none (flush กับ actual bar)
- Fallback `.qnrr-ghost-line` — 2px line เมื่อ run-rate ≈ actual (overperformance)
- Run-rate info อยู่ใน `title` attribute (tooltip hover) ไม่แสดงบน top label

**Partial bar height fix (barH vs projH)**
- เดิม: barH และ projH ใช้ `bm.total_gmv` เหมือนกัน → gapH=0 → ghost ไม่ขึ้น
- ใหม่: pre-compute `rawTotals{}` — barH = raw MTD (`sum curr_gmv rows`), projH = normalized run-rate
- `allGmvs` รวมทั้ง raw และ run-rate เพื่อให้ maxGmv ครอบทั้งคู่

**Top label align**
- Partial bar top-wrap กลับมา 32px เท่ากับ normal months
- แสดงแค่ `฿8.0M mtd` + `17/30d` — ไม่มี run-rate line ซ้อนอีกต่อไป
- `.qnrr-bar-top-wrap.partial` ไม่มี `height: 54px !important` แล้ว

**Transfer in color**
- เปลี่ยนจาก `rgba(255,255,255,.19)` (กลืน bg) → `rgba(64,200,216,.62)` cyan

**Cohort carry-forward (breakdown table)**
- Build `cohortOutlets` set จาก outlet_id ที่ movement_type='new_sales' ใน cohort month
- แต่ละ subsequent month: sum curr_gmv/curr_days×30 ของ outlets กลุ่มนั้น
- เดือนก่อน cohort entry: แสดง '—' สีเทา

---

### 2. QNRR SQL — Data Integrity Investigation

**Root cause ที่พบ**
QNRR Jun GMV ต่างจาก Portfolio ฿417K (5%) ตอนแรก → แต่หลัง verify ด้วย CSV ถูกต้อง diff จริงๆ แค่ ฿2,796 (0.03%) ซึ่งเป็น edge case outlet เดียวที่ `commercial_owner ≠ 'KAM'` ณ วันสั่ง

**ความต่างที่เหลือ (acceptable)**
| KAM | diff |
|-----|------|
| ส่วนใหญ่ | < 0.1% ✓ |
| บางคน | < 1% ~ |
| Total | +0.14% ~ |

**SQL journey (ทั้งหมด fail)**
- v5: LEG D join `dim.user_master` ด้วย `account_id` → error (column ไม่มี)
- v5b: แก้เป็น `res_id` → May (treerak.s) QNRR = Portfolio +125% (dim.user_master เป็น current snapshot ไม่ใช่ historical)
- v6: ลบ `commercial_owner='KAM'` ทุก JOIN → mar_cohort 476 → 2,518 NRR พัง
- v7: คืน mar_cohort + apr_labels กลับ ลบแค่ LEG 2A/3A → May ยังพัง
- **Reverted to v4b** — SQL ที่ถูกต้อง stable

**SQL v4b (current, SHA: 6c95051b6b)**
- `mar_cohort`: `commercial_owner='KAM'` ✓
- `apr_labels`: `commercial_owner='KAM'` ✓
- LEG 2A/3A: `commercial_owner='KAM'` ✓
- ทุก condition ถูกต้อง ไม่ต้องแก้อีก

**CSV ที่ใช้งาน**
- File: `bquxjob_14375d9f_19ed9120ab9.csv`
- Deploy เป็น: `sense_qnrr_2026q2.csv` → R2
- Mar cohort: 2,269 (ทุก KAM รวมกัน — ถูกต้อง, 476 = Ning คนเดียว)
- GMV Jun total diff: +0.14% — acceptable

---

### 3. Mar cohort confusion (clarification)

**476 ≠ total team cohort**
- 476 = Ning คนเดียว (duangruedee.bu) ใน Mar cohort
- Total team Mar cohort = 2,269 outlets (15 KAMs รวมกัน)
- App แสดง 476 เพราะ QNRR sheet กรองเฉพาะ KAM ที่ login อยู่

---

## Files changed (v797)

| File | Change |
|------|--------|
| `src/07c_qnrr_view.js` | ghost bar, barH/projH fix, partial label, cohort carry-forward |
| `src/styles_qnrr.css` | ghost-top, ghost-line, transfer_in cyan, partial 32px |
| `sql/quarterly_nrr_2026_Q2.sql` | reverted to v4b (SHA 6c95051b6b) |
| `sw.js` | sense-v797 |
| `index.html` | built v797 |

---

## Known remaining items

- **treerak.s (May)** Jun diff +0.54% — acceptable เพราะ May เป็น KAM ใหม่ Jun 2026 บาง account ยัง commercial_owner lag
- **guntinun.t (Monet)** Jun diff +0.87% — ยังไม่ได้ investigate
- **SQL commercial_owner lag** — root cause ยังอยู่ (outlet ที่ staff_owner=KAM แต่ commercial_owner≠KAM) ยอมรับ diff < 1% ไปก่อน solution ที่ถูกต้องต้องใช้ historical ownership data ไม่ใช่ current snapshot

---

## Ground truth GMV (locked)
Oct25=188.2M · Nov25=204.4M · Dec25=235.7M · Jan26=214.9M · Feb26=195.1M · Mar26=204.2M · Apr26=192.6M

## NRR verified (Ning, v4b SQL)
Apr=94% · May=98% · Jun=93%
