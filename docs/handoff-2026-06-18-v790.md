# Handoff — 2026-06-18 — v790

## Deploy state
- Version: v790
- Commit: 85af6c7
- SW cache: sense-v790

---

## งานที่ทำใน session นี้

### SQL: quarterly_nrr_2026_Q2.sql → v4

**FIX 4 — expansion ก่อน handover/new_sales**
- `first_dollar_date >= '2026-04-01'` ตรวจก่อน CASE อื่นทั้งหมด
- ป้องกันสาขาใหม่ (iBerry, ปิโตรเลียมไทย ฯลฯ) ถูก misclassify เป็น new_sales เพราะ new_user_exp_date

**FIX 5 — handover/new_sales ต้องมี pre-Mar owner = SALE**
- เพิ่ม CTE `pre_mar_ownership`: ดู `commercial_owner` ล่าสุดก่อน Mar
- handover/new_sales classify ได้ก็ต่อเมื่อ pre-Mar owner = 'SALE' เท่านั้น
- outlet ที่มีแค่ `new_user_exp_date` แต่ไม่ผ่าน Sales → transfer_in

**FIX 6 — comeback ต้องเคยอยู่กับ KAM คนนี้**
- เพิ่ม CTE `kam_last_owner_pre_mar`: ดู last staff_owner + commercial_owner ก่อน Mar
- comeback = `last_commercial_owner = 'KAM' AND last_staff_owner = KAM คนนี้`
- ถ้า last owner เป็นคนอื่น → transfer_in

**FIX — expansion ใน May/Jun LEG**
- LEG 2A (May) และ LEG 3A (Jun): `WHEN al.outlet_id IS NULL AND first_dollar >= '2026-04-01' THEN 'expansion'`
- ป้องกัน outlet ที่เปิดใน May แต่ Jun กลายเป็น new_sales

**Movement definitions (confirmed 2026-06-18):**
- `handover` — pre-Mar owner = SALE + new_user_exp_date = Mar 2026
- `new_sales` — pre-Mar owner = SALE + new_user_exp_date = Apr/May/Jun 2026
- `expansion` — first_dollar_date >= 2026-04-01 (สาขาใหม่ใน Q)
- `comeback` — ไม่มี GMV Mar + last pre-Mar owner = KAM คนนี้ + GMV > 0
- `transfer_in` — active outlet เปลี่ยนมาจาก non-Sales (KAM อื่น/AD/PM/Admin)
- `transfer_out` — Mar cohort ออกไปให้ใครก็ได้

**ผลลัพธ์ที่ verify แล้ว (Ning, CSV v4):**
- May expansion: 7 → 33 outlets (฿765K → ฿791K)
- May new_sales: 77 → 51 outlets (฿899K → ฿287K)
- Jun expansion: 8 → 35 outlets (฿246K → ฿679K)
- Jun new_sales: 64 → 37 outlets (฿1.2M → ฿140K)
- Apr comeback: 18 → 12 outlets (6 ที่ last owner ไม่ใช่ KAM นี้ → transfer_in)

---

### JS: 07c_qnrr_view.js

**QNRR baseMap fix (v785)**
- build baseMap จาก `baseMonthRows` (Apr rows only) แทน scopedRows ทุกเดือน
- แก้: 479 → 476 outlets, ฿13.48M → ฿13.42M hero
- เพิ่ม `handover_base_norm` ใน `_data` object

**Breakdown table Mar column**
- Handover row: แสดง `handover_base_norm` (~฿221K) แทน —
- Total GMV Mar: `base_norm×30 + handover_base_norm` รวม handover จริง
- Bar chart Mar: `marBarTotal` รวม handover สำหรับ height และ label

**Base bar handover layer (v789)**
- เปลี่ยนจาก `position:absolute` div เป็น `qnrr-bar-body` structure เดียวกับ bar อื่น
- `flex-direction: column-reverse`: `coreSeg + hovSeg` → core ล่าง, handover บนสุด
- Bottom-align ถูกต้องเหมือน bar อื่น

**Breakdown table colgroup**
- เพิ่ม `<colgroup>`: Movement col 100px, เดือนละ 58px
- CSS: `table-layout:fixed` บน `.qnrr-bk-table` (v790) — root cause ที่ column ถ่าง

**_qnrrRenderList refactor (v789)**
- Architecture: 1 table ต่อ account มี `<thead>` เดือน 1 ครั้ง, outlet เป็น `<tbody> <tr>`
- Column align ด้วย `table-layout:fixed` + `<colgroup>` fixed 42px
- `outletNameMap2` build นอก loop — ไม่ build ซ้ำทุก account
- `isFirstOutlet` ใช้ `oidx` (forEach index) แทน `indexOf(oid)`
- outlet name ดึงจาก `outletNameMap2` ตอน render แทน `r.account_name`

**Filter bar**
- เปลี่ยนจาก pill (border box) เป็น chip tab underline style (`qnrr-chip`)
- ประหยัดพื้นที่ทางแนวนอน ไม่ตกบรรทัด

**Bar chart partial month**
- label: `17/30d` แทน `17/30 วัน` — สั้นลง ไม่ wrap

**Breakdown color**
- ใช้สีจาก `MV_CFG[mv].color` โดยตรงแทน generic `bk-pos` class
- expansion=teal, new_sales=purple, comeback=amber ถูกต้อง

---

### CSS: styles_qnrr.css

- Cleanup duplicate rules: `.qnrr-ol-tbl` และ `.qnrr-list-filter-bar` ที่ define 2 ครั้ง
- เพิ่ม `.qnrr-acct-tbl`, `.qnrr-acct-tbl-hdr`, `.qnrr-ol-name-cell`, `.qnrr-ol-dot2`, `.qnrr-ol-name2`
- เพิ่ม `.qnrr-chip` (underline tab), `.qnrr-xpand-btn` (ล้อ pv-toggle-btn)
- `.qnrr-base-body`, `.qnrr-base-core-seg`, `.qnrr-base-hov-seg` สำหรับ base bar layers
- `table-layout:fixed` บน `.qnrr-bk-table` (v790)

---

## NRR Logic (confirmed & locked)

**Formula:** `SUM(curr_gmv/curr_days) / SUM(base_gmv/base_days) × 100`
- Denominator = Mar cohort outlets excl. handover (core_nrr + core_nrr_churn)
- Numerator = core_nrr outlets curr_gmv/curr_days เท่านั้น (churn = 0)

**Mar base:** 476 outlets (excl. handover 16), normalized ÷31×30 = ฿13,422,507
**Handover Mar:** 16 outlets = ฿220,685 (แสดงใน chart/table แต่ไม่อยู่ใน NRR denom)
**Hero zone:** แสดง NRR denominator (excl. handover) = ฿13.4M / 476 outlets

**Ning Q2 verification (CSV v4):**
- Apr NRR = 94% ✓
- May NRR = 98% ✓
- Jun NRR = 93% ✓

---

## งานที่ยังค้างอยู่

### Bar chart
- Δ label เทียบ Mar บน bar แต่ละเดือน (ยังไม่ได้ทำ — ตัดสินใจ design ก่อน)
- Bar chart ความสูงแต่ละเดือน diff 1-3% มองไม่เห็น visual difference

### List tab
- Expand all / Collapse all function (`_qnrrExpandAll`) — ยังไม่ได้ test ว่าทำงานถูก

### หน้ากราฟ
- สี Comeback/New Sales/Expansion ใน legend ยังต้องตรวจ

### SQL
- ยังใช้ hardcoded `kam_list` UNNEST — ยังไม่ migrate ไป `freshket-rn.dim.kam_roster`
- Q3 SQL ยังไม่ได้ทำ (ใช้ Q2 อยู่)

---

## Ground truth GMV
Oct25=188.2M · Nov25=204.4M · Dec25=235.7M
Jan26=214.9M · Feb26=195.1M · Mar26=204.2M · Apr26=192.6M
