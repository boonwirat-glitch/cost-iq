# Session Handoff — 2026-06-17 — v755l

## สิ่งที่ทำใน session นี้

### Root cause ที่ค้นพบ (สำคัญมาก)
`04_sku_matcher.js` มี override `_fetchKamBundle` ชื่อ "v207b sequential bundle" ที่ wrap function ตัวจริงใน `02_data_pipeline.js` ทับทั้งหมด — override นี้ fetch แค่ `skus` + `alts` ไม่มี `outlet` เพราะเขียนก่อน session #18 ที่เพิ่ม outlet feature

Fix: เพิ่ม outlet fetch ใน `04_sku_matcher.js` L3196 (non-blocking `.then()`)

### Fixes ที่ deploy แล้ว
- **v755h**: Fix A (`__INVALID__` guard) + Fix B (tab key v2 bump)
- **v755i**: diagnostic logs
- **v755j**: Fix D (retry path ใช้ `_kamOutletLoaded` flag)
- **v755k**: Fix D+E cleanup
- **v755l**: **Fix จริง** — เพิ่ม outlet fetch ใน `04_sku_matcher.js` v207b override

### Tooltip ทำงานแล้ว ✓
- `bulkSkuOutletData` โหลดผ่าน sequential bundle path
- Tooltip แสดงสาขาพร้อม dot สี (เขียว=สั่งแล้ว, แดง=ยังไม่สั่ง)
- `last_order_date` parse ได้ค่าจริง (ทดสอบกับ "น้ำมันฝาม" → 2026-06-04)

### Refactor ที่ควรทำในอนาคต
`04_sku_matcher.js` ไม่ควรมี fetch infrastructure — ย้าย sequential logic กลับเข้า `02_data_pipeline.js` โดยใช้ `shouldAvoidGlobalHeavy()` flag แทน override

---

## งานที่ยังค้างอยู่ (ต้องทำ session ถัดไป)

### 1. UI Tooltip fixes
- เอา label "สั่งแล้ว / ยังไม่สั่ง" ออก — เหลือแค่ชื่อสาขา + dot สี
- เพิ่มความกว้าง tooltip ก่อน truncate (ชื่อร้านยาวโดน crop)
- ไฟล์: `05_kam_view.js` → `_showSkuOutletTooltip()` และ outlet HTML render block

### 2. Tooltip logic expansion — เพิ่ม type ที่ show ⌄
ตอนนี้ `_hasOutlet` guard (L742 ใน `05_kam_view.js`):
```js
s.type === 'gone' || s.type === 'near' || s.type === 'approaching'
```
ต้องเพิ่ม:
- `slow` (ยอดลด) — มี outlet data → ดูได้ว่าสาขาไหนลดลง
- `ordered` / growing / new — SKU ที่สั่งเพิ่มหรือเพิ่งเริ่มสั่ง

### 3. Approaching logic สำหรับ orderCount=1
**ปัญหา:** `last_order_date` จาก SQL1 คือ `MAX(delivery_date)` ของทุกเดือนใน window 2 เดือน
- ถ้า SKU สั่งเดือนนี้ด้วย → `last_order_date` = วันในเดือนนี้ → `expectedDate` = วันนี้เดือนนี้ → logic ผิด
- ควรใช้ `last_order_date` ของเดือนปิดล่าสุดเท่านั้น

**แนวทาง:** แก้ SQL1 ให้ filter `month_date = last_closed_month` ก่อน MAX delivery_date
หรือแก้ใน `06_portview_teamview.js` ให้ใช้ `last_order_date` จาก row ของ last closed month เท่านั้น

### 4. UX copy
- "ยังไม่ถึงรอบ · รออีก N วัน" — สีจางเกินไป อยากให้อ่านชัดขึ้น
- ไฟล์: `05_kam_view.js` → `not_yet` type color/opacity

### 5. Data clarification (ตอบแล้ว — ใช้ implement copy ที่ถูก)
- **"+฿XX proj."** = `(gmv_to_date × days_in_month/days_elapsed) - last_month_gmv` → ส่วนต่างที่คาดว่าจะเพิ่ม
- **"฿897/เดือน"** = gmv ของ SKU นั้นในเดือนปิดล่าสุด (baseline) → ควรแสดงเป็น "เคยสั่ง ฿897/เดือน"

---

## State ของ repo

- Latest deployed: **v755l**
- SW cache: `sense-v755l`
- Snapshot: `snapshot/pre-sku-outlet-fix-v755g`

## Files ที่แก้ใน session นี้
- `src/02_data_pipeline.js` (Fix A, B, D, E — แต่ถูก override โดย 04)
- `src/04_sku_matcher.js` (Fix จริง — outlet fetch ใน v207b override) ← **สำคัญ**
- `sw.js` (v755g → v755l)
- `index.html` (rebuild หลายรอบ)
