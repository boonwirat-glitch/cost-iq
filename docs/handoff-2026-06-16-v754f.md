# Freshket Sense — Handoff 2026-06-16 (v754a→v754f)

## Deployed Version
**v754f** — Latest on freshket-sense.pages.dev

## Snapshot Branch
`snapshot/pre-nrr-export-fix-v753p` — created before this session

---

## สิ่งที่ทำใน session นี้ (ทั้งหมด)

### v754 — Export CSV commission panel + Account ID ใน NRR export
- **Bug:** `_cdsExportCSV` ถูก reference แต่ไม่เคย define → ReferenceError
- **Fix:** เพิ่ม `window._cdsExportCSV` function ใน `07b_cds.js`
- **Fix:** เพิ่ม `Account ID` column ใน `_buildRows()` ของ `07b_nrr_target.js`

### v754a — เพิ่ม Outlet ID (res_id) ใน NRR export
- **Fix:** `o.outletId` = res_id จาก Q5B (CAST(user_id AS STRING))
- เพิ่ม `Outlet ID (res_id)` column ใน portview detail panel + commission panel

### v754b — เพิ่ม Copy TSV button ใน commission panel
- ลบ "Commission ย้อนหลัง" ออก
- ลำดับปุ่ม: CSV → TSV → กฎค่าคอม
- เพิ่ม `window._cdsCopyTSV` function

### v754c — Fix detail panel clip (portview)
- **Bug:** `_frame()` set `maxHeight=400px` ทับ `scrollHeight=949px`
- **Fix:** ใน `_frame()` เช็ค `.tgt-detail-panel.open` → ใช้ `liveH = scrollHeight` แทน

### v754d — ลบ color legend + Fix detail panel ต้อง scroll ก่อนแสดงเต็ม
- ลบ `colorKeySection` (สีของตัวเลข เป้าหมาย block) ออกจาก code ทั้งหมด
- **Fix:** `_tgtToggleDetail` ใช้ double-rAF delay ก่อน `_pvResetExpandedH()` เพื่อให้ layout settle ก่อน measure

### v754e — Fix commission strip ฿7,500 flash (localStorage cache)
- เพิ่ม `window._tgtLoadedFromDB = true` หลัง Supabase fetch สำเร็จ
- แก้ `_tiersReady` ใน `07b_cds.js` ให้ใช้ `_tgtLoadedFromDB` แทน `_tgtLoaded`
- Strip แสดง skeleton จนกว่า DB fetch จะเสร็จ

### v754f — ลบ hardcoded commission tiers (ghost calculator)
- **Root cause:** `_commDefaultTiers()` มี tiers เก่า (฿0/฿5,000/฿7,500) ฝังเป็น fallback
- นี่คือ "commission calculator ตัวที่ 2" ที่เลิกใช้ไปแล้วแต่ยังอยู่ใน code
- **Fix:** `_commDefaultTiers()` return `[]` เสมอ → ไม่มี ghost values อีก

---

## ค้างทำ (Next Session)

### SKU Movement — "ใกล้ถึงรอบ" feature
**Problem:** item ที่สั่ง 1 ครั้ง/เดือน (`avgInterval = 30 วัน`) จะซ่อนอยู่ใน
"ยังไม่ถึงรอบ" ตลอดทั้งเดือน → KAM ไม่เห็น → churn เงียบๆ

**Proposed fix (ตกลงแล้ว):**
- `daysUntil <= 5` → แสดงใน main list เป็น "ใกล้ถึงรอบ" สีเหลืองอำพัน
- `daysUntil > 5` → ซ่อนใน bucket "ยังไม่ถึงรอบ" ต่อไป
- label: "ใกล้ถึงรอบ · อีก N วัน"
- **File:** `src/06_portview_teamview.js` L115-119 (classification block)

### Issues อื่นที่ยังค้าง
- Co-visit GPS verification feature (planned)
- SmartSelect regression (52→41)
- Qty display labels fix
- Echo audio end-to-end validation
- Handover projection formula fix
- Sales commission formula design
- TL Web Dashboard Phase 1

---

## Key Notes สำหรับ session ถัดไป

- `_commDefaultTiers()` return `[]` แล้ว — ถ้ามี KAM ที่ commission แสดงผิดหลัง v754f ให้เช็ค Supabase `commission_rule_tiers` table ก่อน
- `_tgtLoadedFromDB` flag อยู่บน `window` — ถ้าต้อง debug commission strip ใช้ `window._tgtLoadedFromDB` ใน console
- detail panel fix (v754c+d) ทำงานด้วย double-rAF + liveH override — ถ้า panel clip กลับมาให้เช็ค `.tgt-detail-panel.open` ก่อน

