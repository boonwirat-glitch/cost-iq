# Handoff — 2026-06-18 — v793

## Deploy state
- Version: v793
- Commit: c3b6d61
- SW cache: sense-v793

---

## งานที่ทำใน session นี้

### 1. Bar chart — Revert to original design

กลับไปใช้ logic เดิมทั้งหมด ไม่มี zoomed axis:
- `maxGmv` = max ของทุก bar (from 0)
- `barH = Math.round(gmv / maxGmv * chartH)`
- segments = `Math.round(gmv / maxGmv * chartH)` independent
- ghost projection = `Math.round(total_gmv / maxGmv * chartH)`
- `bars-row height` กลับเป็น 178px

### 2. Cohort sub-rows — แยก Apr / May / Jun

เดิม: `└ cohort Apr+` รวม new_sales ทุกเดือน

ใหม่: 3 rows แยกกัน
- `└ cohort มี.ค.` = handover GMV (เดิม)
- `└ cohort เม.ย.` = new_sales ใน by_month['2026-04'] column เม.ย. เท่านั้น
- `└ cohort พ.ค.` = new_sales ใน by_month['2026-05'] column พ.ค. เท่านั้น
- `└ cohort มิ.ย.` = new_sales ใน by_month['2026-06'] column มิ.ย. เท่านั้น

Note: new_sales outlets show GMV only in the month they entered (ไม่ repeat เดือนถัดไป — outlet นั้นจะกลายเป็น core_nrr หรือ churn ใน months ต่อมา) ทำให้แต่ละ cohort row มี value เฉพาะ column ของตัวเอง

Row ถูกซ่อนถ้า `segments['new_sales']` ใน month นั้น = 0

### 3. Sub-row font — ผ่าน CSS ไม่ใช่ inline style

- ลบ `font-size:11px` inline ออกจาก Churn, Contraction, cohort sub-rows ทั้งหมด
- CSS `.qnrr-bk-mv-name.sub`: เอา `font-size` override ออก เหลือแค่ `color`
- font-size inherit จาก `.qnrr-bk-table tr.bk-subrow td` (11px) ทุก row เท่ากัน

---

## Ground truth GMV
Oct25=188.2M · Nov25=204.4M · Dec25=235.7M
Jan26=214.9M · Feb26=195.1M · Mar26=204.2M · Apr26=192.6M
