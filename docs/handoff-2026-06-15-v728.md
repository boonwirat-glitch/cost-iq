# Session Handoff — v728 (2026-06-15)

## Version ปัจจุบัน
**v728** (deployed, Cloudflare Pages auto-deploy)

## Snapshot branch
`snapshot/pre-echo-save-fix-v727`

---

## สิ่งที่ทำใน session นี้

### Goal
แก้ Echo save bug — pipeline เดินครบ 3 steps แต่บันทึกผิด: insert row ใหม่แทน update row เดิม

---

### Root Cause (จาก code จริงผ่าน blob API)

**Bug 1: `_checkRecoverBuffer` ถูกเรียก 2 ครั้งซ้อน**

| แห่ง | Line | |
|---|---|---|
| `_mount()` | 480 | ✅ ถูกต้อง |
| `open()` | 3614 | ❌ ซ้ำ — open() ถูกเรียกได้หลายครั้ง |

**Bug 2: Guard ใน `_checkRecoverBuffer` ขาด `|| _sessionId`**

เดิม: `if (_phase !== 'idle' || _canDebrief()) return;`
แก้แล้ว: `if (_phase !== 'idle' || _sessionId || _canDebrief()) return; // v728`

---

### Fixes (2 จุดใน `09_conv_intel.js`)

Fix 1 — ลบ duplicate setTimeout(_checkRecoverBuffer, 300) ออกจาก open()
Fix 2 — เพิ่ม || _sessionId เข้า guard ของ _checkRecoverBuffer

---

### Files ที่เปลี่ยน

| File | การเปลี่ยน |
|---|---|
| `src/09_conv_intel.js` | Fix 1 + Fix 2 |
| `sw.js` | v727 → v728 |
| `index.html` | rebuild v728 |

---

### สิ่งที่ต้องทดสอบหลัง deploy

1. อัดเสียง → กด "จบ & วิเคราะห์" → รอ pipeline เสร็จ
2. ตรวจใน Supabase: ต้องมีแค่ 1 row ที่ pipeline_stage = 'analyzed' ไม่ใช่ 2 row
3. ทดสอบ nav tap Echo ระหว่าง processing — session ID ต้องไม่หลุด
4. ทดสอบ recovery banner (ปิดแอพระหว่างบันทึก) — ต้องยังแสดงถูกต้อง

---

### สิ่งที่ยังค้างอยู่ (จาก v727)

- Diagnostic console.log ยังอยู่ใน code (เอาออกหลังยืนยัน save ถูกต้องแล้ว)
- UX/UI result panels ยัง raw
- Keyword correction (Freshket/Makro) ยังไม่มี

---

## Ground Truth GMV (locked)
Oct25=188.2M · Nov25=204.4M · Dec25=235.7M · Jan26=214.9M · Feb26=195.1M · Mar26=204.2M · Apr26=192.6M
