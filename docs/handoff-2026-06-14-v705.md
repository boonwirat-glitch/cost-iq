# Session Handoff — v705 (2026-06-14)

## Version ปัจจุบัน
**v705** — TL Dashboard Phase 5: Skills dashboard

## สิ่งที่เพิ่มใน v705

### dash_skills.js (ใหม่)
- Query `skill_definitions`, `user_skill_progress`, `echo_skill_observations`, `profiles`
- Load squad members via `profiles.squad` / `profiles.sale_team` (same logic as Sense `_loadTLSquad`)
- 3 tabs:
  1. **รอประเมิน** — pending list (state='training'), sorted by oldest first, Echo strip ต่อ row
  2. **ภาพรวมทีม** — matrix rep × skill, dot สี pass/train/lock + echo overlay dot
  3. **Echo สัปดาห์นี้** — observations 7 วันล่าสุด พร้อม evidence snippet

### Architecture
- `skillMembers` โหลดจาก profiles.squad (same squad as TL)
- `skillProg` key: `userId:skillId`
- `skillEchoObs` key: `userId:skill_code`
- Loads in parallel กับ commission data ใน initApp

## Phase 6 ต่อไป — Echo integration
- Echo session list per rep
- TL note review
- Co-visit GPS verify trigger

## Files changed
- `src/dashboard/shell_dashboard.html` — added skills slot
- `src/dashboard/dash_layout.js` — loadSkillsData in initApp
- `src/dashboard/dash_skills.js` — NEW Phase 5
- `src/dashboard/styles_dashboard.css` — Skills CSS
- `dashboard.html` — rebuilt v705
