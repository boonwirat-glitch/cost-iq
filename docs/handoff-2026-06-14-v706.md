# Session Handoff — v706 (2026-06-14)

## Version ปัจจุบัน
**v706** — TL Dashboard Phase 6: Echo integration

## สิ่งที่เพิ่มใน v706

### dash_echo.js (ใหม่)
- Query `ci_sessions` สำหรับ team sessions (reuse getTeamEmails จาก skillMembers หรือ portviewBulkData)
- 3 filter tabs: รอรีวิว / ทั้งหมด / Co-visit
- Session card: rep name, account, date/duration, skill dots, tone badge, co-visit badge, review status, TL note preview
- Click card → right detail panel: summary, skill scores, tone, next actions, TL note textarea
- **saveEchoNote()** — PATCH `tl_note` กลับ Supabase ได้จริง
- **markEchoReviewed()** — PATCH `tl_reviewed_at` + `tl_reviewed_by` กลับ Supabase ได้จริง
- Echo nav button เพิ่มใน sidebar

### Architecture
- Load in parallel กับ commission + skills ใน initApp
- getTeamEmails() reuses skillMembers (Phase 5) หรือ portviewBulkData เป็น fallback
- Dedupe logic เหมือน Sense (same owner+account+duration ห่าง < 60s)

## Phase 7 — Polish & production
- Replace mock data ด้วย real CSV (Phase 0 query)
- Performance: lazy load GeoJSON
- Error states, retry logic
- Build pipeline: update build_dashboard.py สำหรับทุก module ใหม่

## Files changed
- `src/dashboard/shell_dashboard.html` — Echo nav + view + script slot
- `src/dashboard/dash_layout.js` — loadEchoData + echo routing
- `src/dashboard/dash_echo.js` — NEW Phase 6
- `src/dashboard/styles_dashboard.css` — Echo CSS
- `dashboard.html` — rebuilt v706
