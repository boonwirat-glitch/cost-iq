# Session Handoff — v710 (2026-06-14)

## Version ปัจจุบัน
**v710** — TL Dashboard Phase 0 complete + deployed

## URL
- **TL Dashboard:** `freshket-dashboard.pages.dev` (Cloudflare Pages project ใหม่)
- **Sense mobile:** `freshket-sense.pages.dev` (ไม่เปลี่ยน)

## Cloudflare Pages setup
- Project: `freshket-dashboard`
- Repo: `boonwirat-glitch/cost-iq`
- Root directory: `dashboard-app`
- Build command: ว่าง
- Auto-deploy จาก branch `main`

## สิ่งที่ทำใน session นี้

### Phase 0 — Real map data
- รับ `fkt_sub_district.csv` (301 rows, 18 hub zones, 244 sub-districts)
- Build แขวง→เขต mapping สำหรับ 50 Bangkok เขต
- Replace `MOCK_DISTRICT` → `REAL_DISTRICT` ใน `dash_map.js`
- **50/50 เขต mapped** — 18 hub zones จากข้อมูลจริง, 1,251 accounts

### Dashboard deployment
- สร้าง `dashboard-app/index.html` — Cloudflare Pages project แยก
- สร้าง `dashboard-app/geo/` — GeoJSON files สำหรับ map
- แก้ `build_dashboard.py` ให้ output ไป `dashboard-app/index.html` ด้วย

## Known issues ที่เหลือ
- Sidebar data (KAM list) ยังเป็น mock — รอ portview.csv จาก R2 load จริง
- GMV per เขต เป็น estimate (account × avg) ยังไม่มี real GMV query per sub_district

## Build workflow (สำหรับ session ต่อไป)
```bash
python3 build_dashboard.py v711
# output: dist/dashboard_v711.html + dashboard-app/index.html
# push ทั้งสองไฟล์ขึ้น git → Cloudflare auto-deploy
```

## Ground Truth GMV (locked)
Oct25=188.2M · Nov25=204.4M · Dec25=235.7M · Jan26=214.9M · Feb26=195.1M · Mar26=204.2M · Apr26=192.6M

## Debug
```js
DashLog.print()  // error log
```
