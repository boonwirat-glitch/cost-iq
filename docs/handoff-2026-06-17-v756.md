# Session Handoff — 2026-06-17 — v756

## สิ่งที่ทำใน session นี้

### Fix A — Approaching orderCount=1 guard (06_portview_teamview.js L123)
**Root cause:** SQL1 ส่ง `last_order_date` = MAX(delivery_date) ของ 2 เดือน (lastClosed + MTD)
ถ้า SKU สั่งเดือนนี้ด้วย → `last_order_date` = วันในเดือนปัจจุบัน → `expectedDate` ในเดือนนี้ → เลยมาแล้ว → classify ผิดเป็น gone/near

**Fix:** เพิ่ม IIFE guard ก่อนเข้า branch orderCount=1 — ถ้า last_order_date อยู่ในเดือนปัจจุบัน → fall through ไป avgInterval logic แทน

### Fix B — Tooltip UX: เอา label ออก + ขยาย maxWidth (05_kam_view.js)
- เอา label "สั่งแล้ว"/"ยังไม่สั่ง" ออกจาก outlet row — เหลือแค่ dot สี + ชื่อสาขา
- ขยาย maxWidth 280px → 340px
- ปรับ gap และ padding ให้อ่านง่ายขึ้น

### Fix C — UX copy: "เคยสั่ง" prefix (05_kam_view.js L738)
- fmt(s.gmv)+'/เดือน' → 'เคยสั่ง '+fmt(s.gmv)+'/เดือน'
- ทำให้ user เข้าใจว่าตัวเลขคือยอดเดิม (baseline)

### Fix D — เพิ่ม slow เข้า _hasOutlet guard (05_kam_view.js L742)
- SKU ประเภท "ยอดลด" ตอนนี้สามารถ tap ชื่อดูสาขาได้แล้ว

---

## State ของ repo

- Latest deployed: **v756**
- SW cache: `sense-v756`
- Snapshot: `snapshot/pre-sku-ui-polish-v755l`

## Files ที่แก้ใน session นี้
- `src/06_portview_teamview.js` (Fix A)
- `src/05_kam_view.js` (Fix B, C, D)
- `sw.js` (v755l → v756)
- `index.html` (rebuild)

---

## งานที่ยังค้างอยู่

### 1. newThisMonth / growing outlet tooltip
SKU ในกลุ่ม "เพิ่งเริ่มสั่ง" และ "สั่งเพิ่มขึ้น" ยังไม่มี tooltip outlet
เพราะ render path อยู่ใน renderKamThisMonth() ไม่ผ่าน renderSkuRow()

### 2. "รออีก N วัน" opacity
Badge approaching สีจางเกิน — 05_kam_view.js L735

### 3. Refactor fetch ออกจาก 04_sku_matcher.js
Blast radius สูง — ทำใน session แยก พร้อม test plan

### 4. +฿XX proj. copy clarification
อาจทำให้ user เข้าใจผิดว่าเป็นยอดจริง — พิจารณาเพิ่ม label อธิบาย
