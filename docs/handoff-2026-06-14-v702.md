# Session Handoff — v702 (2026-06-14)

## Version ปัจจุบัน
**v702** — TL Dashboard Phase 1 complete

---

## สิ่งที่ทำใน session นี้

### Design System (design/ folder)
- `design/tokens.css` — 3-layer token system: Primitive → Semantic → Component, Light + Dark theme
- `design/DESIGN_SYSTEM.md` — spec สำหรับ AI อ่านก่อนทำงานทุก session
- `design/RESPONSIVE.md` — breakpoint rules ทุก component
- `design/CHANGELOG.md` — version history
- `design/components.html` — living reference ทุก component render จริง

### TL Dashboard Phase 1 (src/dashboard/ + dashboard.html)
- `_redirects` — Cloudflare Pages route: `/dashboard → dashboard.html`
- `dashboard.html` — entry point, self-contained 490KB
- `src/dashboard/shell_dashboard.html` — HTML structure
- `src/dashboard/styles_dashboard.css` — desktop CSS ใช้ design tokens
- `src/dashboard/dash_core.js` — Supabase auth, role guard (tl/admin/sales_tl/ad_tl only), utilities
- `src/dashboard/dash_data.js` — R2 CSV pipeline (reuses portview.csv), KAM group builder
- `src/dashboard/dash_layout.js` — view routing (map/team/commission/skills), topbar controls
- `src/dashboard/dash_teamview.js` — sidebar team summary, KAM list, team table, commission, skills
- `src/dashboard/dash_map.js` — D3 choropleth map, Bangkok 50 khet + surrounding, mock data
- `geo/bangkok_khet.geojson` — Bangkok 50 khet polygons (GADM v4.1, simplified)
- `geo/surrounding_provinces.geojson` — Nonthaburi/Pathum Thani/Samut Prakan/Samut Sakhon/Nakhon Pathom
- `build_dashboard.py` — build pipeline (same pattern as build.py)

### Map state
- GeoJSON real: Bangkok 50 Khet (district level) + 5 surrounding provinces
- District data: MOCK — deterministic random seed, realistic GMV ranges
- hub_zone_name mapping: PENDING — needs BigQuery query result from Bucci
- Polygon click → right detail panel with sparkline + metrics + account list

---

## Architecture

```
freshket-sense.pages.dev/          → index.html (Sense mobile, unchanged)
freshket-sense.pages.dev/dashboard → dashboard.html (TL Dashboard desktop)
```

Auth flow:
- Supabase getSession() → fetch profile → check role
- ALLOWED_ROLES: tl, admin, sales_tl, ad_tl (+ aliases)
- Non-allowed → signOut + show error

Layout: topbar (52px) + sidebar (280px) + center (flex-1) + detail drawer (340px)

---

## Known state

### Working
- Auth guard, login form, role check
- 3-panel desktop layout
- Map: Bangkok choropleth, hover tooltip, click detail panel, zoom controls
- Scope toggle: กรุงเทพฯ / กทม.+ปริมณฑล
- Metric toggle: GMV / Accounts / Outlets / New Acc
- Month selector: Nov25–Apr26
- Sidebar: team summary, KAM list, pace bars
- Team table view
- Commission estimate view
- All data from portview.csv (same R2 as Sense)

### Mock data (replace when real CSV ready)
- Map district values: MOCK_DISTRICT in dash_map.js
- Account list in detail panel: hardcoded mock

### Pending (Phase 2)
- hub_zone_name → Khet mapping (needs BigQuery query)
- Real map CSV: tl_map_data.csv (aggregate per sub_district_th × hub_zone_name × month)
- Replace MOCK_DISTRICT with real data from R2
- Co-visit GPS verify feature (Sense, separate)

---

## Files changed
- New: `_redirects`, `dashboard.html`, `build_dashboard.py`
- New: `src/dashboard/` (7 files)
- New: `geo/` (2 GeoJSON files)
- New: `design/` (5 files)
- Sense mobile: **ไม่แตะเลย**

