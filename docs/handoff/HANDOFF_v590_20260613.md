# Freshket Sense — Session Handoff v590

**repo:** `github.com/boonwirat-glitch/cost-iq` · branch: `main`
**HEAD:** `a3f8daa` · **date:** 13 มิ.ย. 2569
**snapshot ก่อน session:** `snapshot/pre-prompt-trim-v589`

---

## Versions Shipped This Session (v590)

| Version | เรื่อง |
|---|---|
| v590 | **Prompt trim** — ตัด `_buildGeminiPrompt()` ใน `src/09_conv_intel.js`: ลบ account context (ชื่อร้าน/GMV/churn), กฎเหล็ก 5 ข้อ, ตัวอย่าง no_speech JSON เต็ม, key_moments instruction "กระจายครอบคลุมทั้งไฟล์/ไม่เกิน 30 จุด", OCPB กฎเก็บ fact ยาว, rubric echo_observable+principle_th · เหลือ rubric เกณฑ์ผ่านสั้น + OCPB 4 dims + schema โครงเปล่า + no_speech guard 1 บรรทัด · แก้ hallucination แบบ degenerative loop (โมเดลแต่งบทสนทนาจาก prompt context แทนเสียงจริง) |

---

## สาเหตุ Hallucination ที่แก้

Root cause: v585 ถอด hard cap + Worker v2 เพิ่ม maxOutputTokens 8192→16384 + prompt มี account context ครบ (ชื่อร้าน/segment/GMV) = โมเดลแต่งบทสนทนาจาก prompt แทนเสียงจริงเมื่อเสียงไม่ชัด หลักฐาน: quote "ร้าน Thb" ใน key_moments คือชื่อจาก account context ไม่ใช่จากเสียง

---

## Known Issues ค้างจาก Session นี้ (ไม่ได้แตะ)

| จุด | รายละเอียด |
|---|---|
| Topbar ดันทับ status bar | `_minimize()` ปล่อย body scroll lock ค้าง — `document.body.style.position='fixed'` ไม่ถูกคืนค่า |
| `echo-active` class leak | ค้างทั้งแอพเมื่อ analyze fail หรืออัดสั้น — path `_onStop` / `_processBlob` fail เรียก `_unmount()` ตรงโดยไม่ remove class |
| ขอบขาวล่างจอ dim mode | v588 แก้ด้วย `body.echo-active{background:#111}` แต่ echo-active leak ทำให้ยังเจอได้ |
| Result screen plain text | `sd2-*` CSS inject เฉพาะตอนเปิด session detail — result screen render ก่อน inject เลยไม่มี style |
| Session detail ≠ result screen | render คนละ function คนละ CSS ชุด ได้ดีไซน์ต่างกัน |
| Recovery ไม่คืน GPS | `_recoverBuffer()` ไม่ restore `ci_checkin_cache` → session กู้คืนได้ `rep_lat=null` |
| no_speech เข้า history | `_processBlob` save ก่อน check `no_speech` → session เปล่าใน ci_sessions |

---

## Action Items ค้างจาก v589

1. **Handover commission parse bug** — `byNewKamName` ใน `02_data_pipeline.js` ไม่ parse p[10]-p[15] → commission = ฿0
2. **Commission snapshots audit** — ก่อน v559 engine ใช้ default params
3. **Re-run SQL2 + re-export KAM bundles** — partition bug fix รอ
4. **SmartSelect 52→41 regression**
5. **Qty unit display** — "3 kg" แทน "3 ถัง"
