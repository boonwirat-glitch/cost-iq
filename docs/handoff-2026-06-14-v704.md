# Session Handoff — v704 (2026-06-14)

## Version ปัจจุบัน
**v704** — TL Dashboard Phase 4: Commission engine

## สิ่งที่เพิ่มใน v704

### dash_commission.js (ใหม่)
- Query `commission_payout_snapshots` จาก Supabase — pre-computed results จาก Sense
- Query `commission_plans` + `commission_rule_tiers` สำหรับ bracket display
- Fallback: ถ้าไม่มี snapshot → estimate จาก pace × rate bracket (Sales TL table จาก PDF)
- 6-month trend sparkline (จาก snapshots หรือ estimate)
- Per-KAM breakdown table — NRR %, commission, snapshot_status

### Architecture note
- ไม่ port computation logic จาก 07a_commission_engine.js ทั้งหมด
- Dashboard reads `commission_payout_snapshots` ที่ Sense เขียนไว้แล้ว (single source of truth)
- Fallback estimate ใช้ Sales TL bracket table จาก Commission PDF (Apr 2026)

### Month format
- Display: "Apr 26", ISO: "2026-04"
- MONTH_ISO map ใน dash_commission.js

## Phase 5 ต่อไป — Skills dashboard
- Query `skill_definitions` + `user_skill_progress` + `echo_skill_observations`
- Rep progression matrix (gate 1/2/3)
- TL pending assessments
- Echo session count per rep

## Files changed
- `src/dashboard/shell_dashboard.html` — added commission slot
- `src/dashboard/dash_layout.js` — loadCommissionData in initApp
- `src/dashboard/dash_teamview.js` — removed old renderCommissionView
- `src/dashboard/dash_commission.js` — NEW Phase 4
- `dashboard.html` — rebuilt v704
