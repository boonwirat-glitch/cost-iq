# Handoff — 2026-06-18 — v784

## Deploy state
- Version: v784 (sense-v784)
- Commit: e0bdfa2cb58b
- SW cache: sense-v784

## งานที่ทำใน session นี้

### v775 — QNRR Redesign
- ออกแบบ design ใหม่ทั้งหน้า: hero zone, toggle chart/breakdown, color palette
- CSS: semantic movement colors (core=green, handover=blue, expansion=teal, new=purple, comeback=amber, churn=red)
- Shell HTML: restructure zones A/B/C/D

### v776 — Ghost bar fix + base label
- แก้ `overflow:hidden` บน `bar-chart-area` และ `bars-row` กัน ghost bar ล้น
- Bar top label: GMV only ไม่ repeat NRR%

### v777 — Breakdown table redesign
- Font ใหญ่ขึ้น (8px→11-12px)
- เพิ่ม Core NRR sub-rows: active / churn / net core

### v778 — Churn segments fix
- แก้ `core_nrr_churn` และ `transfer_out` ใช้ `base_gmv` แทน `curr_gmv` (เพราะ curr=0 เสมอ)

### v779 — Contraction sub-row
- เพิ่ม Contraction sub-row ใน breakdown table
- Contraction = `segments.core_nrr - core_nrr_base_sum`
- สีแดงถ้าลดลง สีเขียวถ้าเพิ่ม

### v780 — Layout relayout
- Tab กราฟ: bar chart + breakdown stacked ในหน้าเดียว
- Tab List (แทน Breakdown): full-quarter account × outlet list
- Buttons ใหญ่ขึ้น (vtog 9px→12px, mv-btn 8px→11px)
- Rename Contraction → Down/Up

### v781 — List tab fixes
- แก้ `_qnrrRenderList` อยู่นอก IIFE ทำให้ `_el` ไม่ defined → ย้ายเข้าใน IIFE
- Filter bar single row (overflow-x: auto)
- List: outlet มี 4-column GMV table แทน mini bar
- ลบ dot หน้า account name

### v782 — _el scope fix
- ย้าย `_qnrrRenderList` + `_qnrrListFilter` เข้าใน IIFE
- Filter bar single row CSS

### v783 — Normalize ÷days×30 ทุกตัว
- `segments[mv]` normalize ด้วย `curr_gmv/curr_days*30`
- `churn/transfer_out` normalize ด้วย `base_gmv/base_days*30`
- `contraction` normalize ทั้งสองฝั่ง
- ลบ Net core row ออก
- Partial month: `is_partial` flag จาก `days_in_month` จริง
- Bar partial month แสดง actual raw + `X/31 วัน`
- Breakdown/List header: `~` สำหรับ partial month

### v784 — Exclude handover from NRR denominator (JS only)
- `baseMap` เพิ่ม `&& r.movement_type !== 'handover'`
- ฐาน hero เปลี่ยนจาก 488 outlets → 479 outlets
- SQL v3 ยัง unchanged (handover base_gmv ยังส่งออกมาปกติ)
- Hero sub-label: `excl. handover · ÷days×30`

---

## งานที่ยังค้างอยู่ (ยังไม่ได้ทำ)

### List tab
- ลบ label "CORE NRR" ออกจาก outlet row
- ย้าย month header (มี.ค./เม.ย./พ.ค./มิ.ย.) ขึ้นไปอยู่ที่ thead
- ปุ่ม expand all / collapse all
- Filter buttons ยังตกบรรทัด

### Breakdown table
- เพิ่ม Handover มี.ค. column (base_gmv normalized)
- Color code ของ Comeback/New Sales/Expansion ไม่ถูก assign

### Bar chart
- `406 out · 15/30d` ยาวเกินแตก 2 บรรทัด
- ความแตกต่างระหว่าง bar แต่ละเดือนน้อยมาก (1-3%) ยังไม่ได้ handle

### NRR/Reconcile logic (ค้างไว้ คุยแล้วยังไม่ได้ข้อสรุป)
- ฐานที่ถูกต้องควรเป็นเท่าไหร่ — JS v784 ใช้ 479 outlets (excl. handover)
- Contraction label และ formula ยังต้องตกลงให้ชัดก่อน build

### SQL
- SQL v3 อยู่ใน repo (reverted จาก v4)
- SQL v4 ที่ลองแก้ล้มเหลวเพราะ mar_cohort ใช้ร่วมกับ LEG ทั้งหมด การกรอง handover ออกจาก mar_cohort ทำให้ handover rows ไม่มี base_gmv

### Bar chart design
- ยังไม่ได้ตัดสินใจระหว่าง Waterfall / Dual-axis / Zoom Y-axis

---

## Ground truth GMV
Oct25=188.2M · Nov25=204.4M · Dec25=235.7M
Jan26=214.9M · Feb26=195.1M · Mar26=204.2M · Apr26=192.6M
