{
  "package": "freshket-sense-v202",
  "version": "202.0.0",
  "baseline": "v202-per-kam-bundle",
  "productBaseline": "v194-auth-fix",
  "notes": [
    "v202: Per-KAM bundle architecture — load sense_skus/alts_[email].csv (~4MB) instead of bulk 120MB",
    "v202: KAM role pre-loads own bundle silently on login",
    "v202: TL pre-warms bundle when drilling into KAM (teamviewDrillKam)",
    "v202: ensureSenseData: bundle-first path with 10s timeout, fallback to bulk",
    "v202: IndexedDB cache for bundles (bundle-skus/alts-[safeKey], 6h TTL)",
    "v202: _kamBundleLoaded Set + _kamBundleInFlight dedup (concurrent fetch safe)",
    "v202: reloadFromCloudflareR2 clears bundle state on explicit refresh",
    "v201f: Dynamic KAM mapping in all 8 SQLs (replaces hardcoded 623-row list)",
    "v201e: Fix Q10 portview_handover cur_month QUALIFY bug (double-transfer)",
    "v201d: Alternatives moved to Tier 2 background sequential",
    "v201c: Sense Gate animation starts immediately on tap"
  ]
}
