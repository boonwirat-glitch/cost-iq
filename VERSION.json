{
  "package": "freshket-sense-v155-phase22-2-auth-fix",
  "version": "155.22.2",
  "baseline": "v155-phase22-olive-chat-v2",
  "productBaseline": "v155-phase6.2-chat-grounding",
  "notes": [
    "Fix: SW skipWaiting removed — prevented iOS auth interruption during SW update",
    "Fix: pageshow handler added — iOS bfcache restore now re-validates session",
    "Fix: TOKEN_REFRESHED handled in onAuthStateChange — currentUser stays in sync on mobile",
    "Fix: visibilitychange 2s grace period — prevents false login flash during token refresh",
    "Fix: loginTransitionRunning 4s safety valve — prevents hang + auto-login loop",
    "Phase 22.1: Default Cloudflare AI proxy URL for mobile/new devices",
    "Phase 22: Olive Chat v2 cross-scope context router"
  ]
}
